# این پروژه با pyproject.toml پکیج می‌شود (pip install -e .).
# این فایل فقط برای راحتیِ تنظیم سریعِ Interpreter در PyCharm است و باید با
# dependencies در pyproject.toml هم‌گام بماند.
PySide6>=6.6
sqlalchemy>=2.0
psycopg2-binary>=2.9
python-dotenv>=1.0
alembic>=1.13
jdatetime>=5.0
