# این پروژه با pyproject.toml پکیج می‌شود (pip install -e .).
# این فایل فقط برای راحتیِ تنظیم سریعِ Interpreter در PyCharm است و باید با
# dependencies در pyproject.toml هم‌گام بماند.
PySide6>=6.6
sqlalchemy>=2.0
psycopg2-binary>=2.9
python-dotenv>=1.0
alembic>=1.13
jdatetime>=5.0
openpyxl>=3.1
pdfplumber>=0.11
pytesseract>=0.3.10
pypdfium2>=4.0
# نکته: pytesseract فقط یک پوستهٔ Python است -- خودِ برنامهٔ Tesseract OCR
# باید جداگانه رویِ سیستم نصب شود (رایگان/متن‌باز، مجوزِ Apache-2.0):
# https://github.com/UB-Mannheim/tesseract/wiki -- هنگامِ نصب، بستهٔ
# زبانِ «Persian» را هم انتخاب کنید تا کدها/توضیحاتِ فارسی هم خوانده شوند.
