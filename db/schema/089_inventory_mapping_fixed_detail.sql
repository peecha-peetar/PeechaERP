-- پیچا | طبقِ گزارشِ صریح: حسابِ نقش‌محورِ مالياتِ خرید (یا هر حسابِ
-- نقش‌محورِ دیگرِ انبار) ممکن است خودش یک تفصیلیِ الزامی داشته باشد که
-- نه از سرِسند (مرکزِ هزینه/پروژه) و نه از طرفِ‌حساب/کالایِ ردیف قابلِ
-- تامین است -- و اغلب هم همیشه یک تفصیلیِ *ثابت* است (مثلاً یک ردیفِ
-- تعریف‌شده برایِ «مالياتِ بر ارزش‌افزوده»). این ستون همان تفصیلیِ
-- ثابتِ ازپیش‌تخصیص‌یافته را نگه می‌دارد -- دقیقاً هم‌الگو با
-- treasury.account_mappings.detail_account_id.

ALTER TABLE inv.account_mappings
    ADD COLUMN detail_account_id INT NULL REFERENCES acc.detail_accounts(detail_account_id);
