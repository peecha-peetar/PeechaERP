-- پیچا | اعمالِ GeoFence رویِ ثبتِ ویزیت (R219، بخشِ ۴/۹) -- طبقِ اصلِ
-- صریح («هرگز یک عملیاتِ واقعی را برایِ نادقیقیِ GPS رد نکن»): فقط
-- پرچمِ غیرِمسدودکننده -- خودِ ثبتِ ویزیت هرگز رد نمی‌شود، فقط برایِ
-- گزارش/هشدار به مدیر علامت‌گذاری می‌شود.

ALTER TABLE comm.customer_visits
    ADD COLUMN is_outside_geofence BOOLEAN NULL;
