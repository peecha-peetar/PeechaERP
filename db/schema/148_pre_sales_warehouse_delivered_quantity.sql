-- طبقِ درخواستِ صریحِ کاربر (روالِ پخشِ سرد): انباردار باید بتواند سفارش
-- را باز کند و مقدارِ واقعیِ تحویلی/توزین‌شدهٔ هر ردیف (وزنی یا تعدادی)
-- را وارد/ادیت کند -- این مقدار ممکن است با مقدارِ سفارش‌داده‌شده فرق
-- کند (کسری/تفاوتِ وزن). این مقدار بعداً به‌جایِ مقدارِ سفارش، مبنایِ
-- پیش‌فرضِ «تبدیل به فاکتور» قرار می‌گیرد.

ALTER TABLE comm.commercial_document_lines
    ADD COLUMN warehouse_delivered_quantity NUMERIC(18, 6);
