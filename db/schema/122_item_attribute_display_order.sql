-- طبقِ درخواستِ صریح («برایِ تعریفِ متغیرها چند ویژگی داریم، الویت و
-- ترتیبشون چجوری مشخص میشه؟»): تا اینجا ترتیبِ ویژگی‌ها فقط بر اساسِ
-- ترتیبِ ساختِ آن‌ها بود -- این ستون امکانِ تعیینِ صریحِ اولویت/ترتیب را
-- می‌دهد (هم برایِ نمایش در فهرست، هم برایِ ترتیبِ بخش‌هایِ نامِ خودکارِ
-- متغیر، مثلاً «سایز / رنگ / مدل»).
ALTER TABLE inv.item_attributes ADD COLUMN display_order SMALLINT NOT NULL DEFAULT 0;
