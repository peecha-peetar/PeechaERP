-- طبقِ درخواستِ صریح: در تنظیماتِ هر روشِ ردیفِ سندِ دریافت/پرداخت (نقد/بانک/
-- تخفیف/کالابرگ/بن/تهاتر...)، جدا از معین، بشود یک تفصیلیِ مشخص هم از پیش
-- تخصیص داد — اگر تخصیص داده شود، دیگر در فرمِ سند دوباره پرسیده نمی‌شود؛
-- اگر تخصیص داده نشود، کاربر خودش در فرمِ سند تفصیلی را انتخاب می‌کند.
ALTER TABLE treasury.account_mappings
    ADD COLUMN detail_account_id INT NULL REFERENCES acc.detail_accounts(detail_account_id);
