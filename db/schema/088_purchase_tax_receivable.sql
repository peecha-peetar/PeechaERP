-- پیچا | طبقِ گزارشِ صریح: در فاکتورِ خرید، مالیاتِ ردیف محاسبه می‌شد
-- (comm.commercial_document_lines.tax_amount) ولی هرگز به سندِ حسابداری/
-- انبار منتقل نمی‌شد -- چون inv.stock_document_lines هیچ ستونِ tax_amount
-- نداشت، و موتورِ Post برایِ رسیدِ خرید فقط بهایِ واحدِ خام (بدونِ کسرِ
-- تخفیف/افزودنِ مالیات) را به‌عنوانِ بستانکاریِ حساب‌هایِ پرداختنی
-- می‌فرستاد. این ستون، پُلِ لازم برایِ عبورِ مبلغِ مالیاتِ هر ردیف تا
-- موتورِ حسابداریِ انبار است.

ALTER TABLE inv.stock_document_lines
    ADD COLUMN tax_amount NUMERIC(18, 2) NOT NULL DEFAULT 0;
