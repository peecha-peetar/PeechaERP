-- Field Sales: طبقِ درخواستِ صریحِ کاربر («تعیینِ کانالِ مجزا برایِ پخشِ
-- سرد و گرم» + «پروسه‌یِ پخشِ سرد از گرم جدا شود»): اپِ موبایل تا امروز
-- برایِ همه‌یِ ویزیتورها بدونِ استثنا صفحه‌یِ سفارش را با
-- channelTypeCode="VAN_SALES" هاردکد نشان می‌داد -- یعنی حتی ویزیتورِ
-- پخشِ سرد هم مجبور بود فاکتورِ آنیِ پخشِ گرم بزند، نه صرفاً سفارش ثبت
-- کند. این ستون، نوعِ کانالِ ثابتِ هر کاربر را برایِ هر شرکت مشخص
-- می‌کند (تنظیم از دسکتاپ -- نه سوییچِ دستیِ خودِ ویزیتور در گوشی)،
-- هم‌الگو با voip_extensionِ موجود رویِ همین جدول.

ALTER TABLE sec.user_companies
    ADD COLUMN mobile_channel_type_code VARCHAR(15);
