-- طبقِ درخواستِ صریح: فرمِ چکِ پرداختی باید «شماره‌یِ صیادی» (شناسه‌یِ
-- ۱۶رقمیِ چاپ‌شده رویِ هر چکِ صیادی، مستقل از شماره‌یِ شبا) را هم ثبت
-- کند — این ستون همان مقدار را نگه می‌دارد.

ALTER TABLE treasury.issued_checks
    ADD COLUMN sayad_no VARCHAR(20) NULL;
