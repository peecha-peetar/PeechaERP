-- پیچا | ماژولِ فروشِ اینترنتی -- طبقِ درخواستِ صریحِ کاربر («حالت‌هایِ
-- موجودی» به‌ازایِ هر کالا برایِ نمایش در فروشگاهِ اینترنتی): پیش‌فرض
-- «بر اساسِ دیتابیس» یعنی موجودیِ واقعیِ انبار فرستاده می‌شود (رفتارِ
-- فعلی)؛ «همیشه موجود» یعنی صرفِ‌نظر از موجودیِ واقعی همیشه قابلِ‌سفارش
-- نشان داده شود (مثلاً کالایِ دیجیتال/دائماً‌موجود)؛ «ناموجود» یعنی
-- فروشِ اینترنتیِ این کالا موقتاً متوقف شود بدونِ تغییرِ خودِ موجودیِ ERP.
ALTER TABLE inv.items ADD COLUMN ecommerce_stock_mode VARCHAR(20) NOT NULL DEFAULT 'DATABASE'
    CHECK (ecommerce_stock_mode IN ('DATABASE', 'ALWAYS_IN_STOCK', 'OUT_OF_STOCK'));
