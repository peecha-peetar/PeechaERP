-- طبقِ درخواستِ صریح: عکس/فایلِ ضمیمهٔ پرداخت‌هایِ سفارش باید برایِ همه‌یِ
-- کاربرانِ شبکه قابل‌دیدن باشد -- یعنی مسیرِ ذخیره‌سازیِ محلی (پوشهٔ
-- تنظیماتِ همان کامپیوتر) دیگر کافی نیست. این ستون یک مسیرِ اشتراکیِ
-- شبکه‌ای (مثلاً \\SERVER\Share\PeechaAttachments) را نگه می‌دارد که
-- همهٔ کاربرانِ متصل به همین دیتابیس از آن استفاده می‌کنند؛ اگر خالی
-- باشد، رفتارِ قبلی (پوشهٔ محلیِ تنظیمات) ادامه می‌یابد.
ALTER TABLE comm.order_tracking_settings ADD COLUMN attachments_dir VARCHAR(500) NULL;
