-- Field Sales: طبقِ درخواستِ صریحِ کاربر («برای ویزیت پخش سرد هم ویزیت
-- و عکس و سفارش باشه») -- تا امروز تنها راهِ عکس‌گرفتن در اپِ موبایل
-- رسیدِ تحویلِ پخشِ گرم بود (delivery_confirmations.photo_base64)؛ پخشِ
-- سرد اصلاً عکسی نداشت. این ستون یک عکسِ ساده (مثلاً از مغازه/ویترین)
-- را در همان لحظهٔ تکمیلِ ویزیت ثبت می‌کند -- برایِ هر دو نوعِ پخش
-- در دسترس است (اختیاری)، نه فقط پخشِ سرد.

ALTER TABLE comm.customer_visits
    ADD COLUMN photo_base64 TEXT;
