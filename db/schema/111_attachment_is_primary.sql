-- طبقِ درخواستِ صریح («باید برای هر تفضیلی چند تا عکس هم بتونیم بزاریم...
-- یکعکس بعنوان عکس اصلی نمایش داده بشه»): هر حسابِ تفصیلی می‌تواند چند
-- عکس/فایل داشته باشد؛ از میانِ عکس‌های فعال، دقیقاً یکی می‌تواند «عکسِ
-- اصلی» باشد (برایِ بندانگشتیِ کنارِ نام و اولین نمایِ زوم).

ALTER TABLE doc.attachments
    ADD COLUMN is_primary BOOLEAN NOT NULL DEFAULT FALSE;
