-- پیچا | رفعِ باگ: پس از ادغامِ فرمِ «کالا و خدمت» در گروهِ تفصیلیِ کالا،
-- تعریفِ کالا فقط در سطحِ‌آخرِ سلسله‌مراتبِ گروه ممکن است (is_leaf_level)؛
-- ولی سقفِ پیش‌فرضِ تعدادِ سطح برایِ همه‌یِ نوع‌بُعدهایِ تخصصی (ازجمله
-- INVENTORY_ITEM) از 013_uniform_detail_level_digits.sql برابرِ ۴ است، یعنی
-- کاربرِ تازه‌کار باید اول ۴ سطح دسته‌بندی بسازد تا فیلدهایِ کالا اصلاً
-- نمایان شوند — دقیقاً همان علتِ گزارشِ کاربر («فقط کد و نام والد را
-- نشان می‌دهد»). پیش از ادغام، کالاها فرمِ کاملاً مستقلِ خودشان را
-- داشتند و این محدودیت اصلاً وجود نداشت. سقفِ کالا به ۱ برمی‌گردد
-- (بدونِ پیش‌نیازِ ساختِ دسته‌بندی) — کسی که واقعاً به دسته‌بندیِ چندسطحی
-- نیاز دارد هم‌چنان می‌تواند از صفحه‌یِ «گروه‌هایِ تفصیلی» تعدادِ سطح را
-- افزایش دهد.
UPDATE acc.detail_dimension_types
SET max_level_no = 1
WHERE code = 'INVENTORY_ITEM' AND max_level_no = 4;
