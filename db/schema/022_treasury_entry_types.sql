-- پیچا | افزودنِ دو نوعِ سندِ تازه برایِ ماژولِ خزانه‌داری: «دریافت» و
-- «پرداخت» — طبقِ درخواستِ صریحِ کاربر برایِ شروعِ ماژولِ خزانه‌داری، رویِ
-- همان موتورِ اسنادِ حسابداریِ موجود (اسنادِ خزانه‌داری همان اسنادِ
-- حسابداری‌اند، فقط entry_type متفاوت دارند تا در گزارش/فهرستِ جداگانه
-- قابلِ‌تفکیک باشند).
-- پیش‌نیاز: 003_accounting_core.sql

INSERT INTO acc.journal_entry_types (entry_type_id, code) VALUES
    (5, 'RECEIPT'), (6, 'PAYMENT')
ON CONFLICT (entry_type_id) DO NOTHING;
