# ماژول حسابداری (دفتر کل) — پیچا

اسکیمای مرتبط: `db/schema/003_accounting_core.sql`، `db/schema/004_workflow_cartable.sql`

## ساختار کدینگ حسابداری (۴ سطح)

1. **گروه** (`account_level = 1`)
2. **کل** (`account_level = 2`)
3. **معین** (`account_level = 3`) — این سه سطح یک درخت والد/فرزند معمولی‌اند (`parent_account_id`)
4. **تفصیلی شناور** — عمداً به‌صورت یک سیستم جدا (`detail_dimension_types` / `detail_accounts` / `account_detail_dimensions`) مدل شده، نه یک والد/فرزند دیگر در همان درخت؛ چون برخلاف سه سطح بالا، یک معین می‌تواند هم‌زمان چند بعد تفصیلی مختلف بخواهد (مثلاً هم «مشتری» هم «مرکز هزینه»)، که در یک درخت تک‌والدی امکان‌پذیر نیست.

جدا از این ۴ سطح، هر حساب یک **نوع حساب** هم دارد (`acc.account_types`):
- **ترازنامه‌ای (PERMANENT)** — دارایی/بدهی/سرمایه؛ در پایان سال مالی بسته نمی‌شود.
- **موقت (TEMPORARY)** — درآمد/هزینه؛ با سند اختتامیه به صفر می‌رسد.

این معمولاً در سطح گروه تعیین می‌شود و کل/معینِ زیرمجموعه‌اش همان مقدار را دارند (بررسی هم‌خوانی در سرویس پایتون، نه در دیتابیس، چون این‌طور بررسی بین‌ردیفی/بازگشتی است).

## شماره‌گذاری سند (دوگانه)

هر سند دو شماره دارد:
- **`temporary_no`** (شماره موقت): از لحظه‌ی ایجاد سند پر می‌شود؛ قابل تغییر و قابل ادغام (وقتی دو سند موقت با هم ادغام می‌شوند، یکی از آن‌ها وضعیت `CANCELLED` می‌گیرد).
- **`permanent_no`** (شماره ثابت): فقط هنگام تایید نهایی (تبدیل وضعیت به `PERMANENT`) تخصیص می‌یابد؛ باید پیوسته (بدون پرش) باشد و پس از تخصیص **غیرقابل تغییر** است — این عدم‌تغییرپذیری با یک تریگر (`tr_journal_entries_prevent_permanent_no_change`) در همان دیتابیس تضمین شده، نه فقط در کد اپلیکیشن.

تخصیص خودِ عدد بعدی (پیوسته نگه‌داشتنش زیر بار همزمانی چند کاربر) باید در سرویس پایتون با یک تراکنش قفل‌دار (مثلاً `UPDLOCK`/`HOLDLOCK` روی یک ردیف شمارنده) انجام شود؛ این بخشِ الگوریتمی است، نه ساختار جدول.

## کارتابل (تایید سند + حذف سند)

تبدیل سند از `TEMPORARY` به `PERMANENT` دیگر یک تغییر وضعیت مستقیم توسط تنظیم‌کننده‌ی سند نیست؛ سند برای تایید وارد **کارتابل** می‌شود (`wf.cartable_items` با `request_type_id = CREATE`، جزئیات کامل در `docs/cartable-workflow.md`). این کارتابل مخصوص حسابداری نیست — سراسری است و اسناد همه‌ی ماژول‌ها (انبار، خرید و فروش، خزانه‌داری، ...) از همان مسیر عبور می‌کنند، چون به‌جای اشاره‌ی مستقیم به `acc.journal_entries`، به `sec.forms` (کاتالوگ فرم‌های سیستم) + یک `source_record_id` عمومی ارجاع می‌دهد.

**حذف سند دائم** هم مستقیم انجام نمی‌شود؛ یک درخواست جدید با `request_type_id = DELETE` وارد همان کارتابل می‌شود و فقط با تایید، سرویس حسابداری سند را (معمولاً با تبدیل به `CANCELLED` به‌همراه یک سند برگشتی خودکار، نه حذف فیزیکی ردیف) غیرفعال می‌کند — تا تاریخچه‌ی حسابداری همیشه قابل ردیابی بماند.

**عودت به صادرکننده**: اگر تاییدکننده در هر مرحله سند را «عودت» بدهد، آیتم کارتابل `PENDING` می‌ماند ولی `current_approver_user_id` آن به تنظیم‌کننده‌ی اصلی سند برمی‌گردد؛ او سند را اصلاح و با اقدام `RESUBMIT` دوباره به همان مرحله ارسال می‌کند.

**ضمائم**: فایل‌های پیوستِ هر سند (مثلاً تصویر فاکتور) با همان الگوی `form_id` + `source_record_id` در `doc.attachments` ذخیره می‌شوند (جزئیات در `db/schema/005_attachments.sql`).

## سند اختتامیه (دستی + خودکار)

- **دستی**: کاربر مثل هر سند دیگری، با `entry_type_id = CLOSING`، ردیف‌ها را خودش وارد می‌کند.
- **خودکار**: سرویسی که تمام حساب‌های `account_type_id = TEMPORARY` را با مانده‌ی غیرصفر در سال مالی پیدا می‌کند، هرکدام را در برابر `acc.company_accounting_settings.profit_and_loss_account_id` صفر می‌کند، سپس مانده‌ی همان حساب واسط را به `retained_earnings_account_id` منتقل می‌کند. سند تولیدشده `is_system_generated = TRUE` می‌گیرد (برای تمایز در گزارش‌ها/حسابرسی) ولی از نظر ساختاری یک سند عادی با `entry_type_id = CLOSING` است — بعد از تولید هم مثل هر سند دیگر باید از کارتابل تایید شود (چون هنوز `TEMPORARY` است).

## کجا در دیتابیس enforce نشده و چرا

قوانین چندردیفی/گردش‌کاری هم‌چنان در سرویس پایتون بررسی می‌شوند (نه در DB)، به‌جز مواردی که صراحتاً به تریگر/قید سپرده شده (مثل غیرقابل‌تغییر بودن `permanent_no`):

1. تراز بودن سند (مجموع بدهکار = بستانکار) — فقط لحظه‌ی تایید در کارتابل بررسی می‌شود.
2. `is_postable = TRUE` بودن حساب مقصد هر ردیف.
3. تکمیل ابعاد تفصیلیِ الزامی هر ردیف.
4. باز بودن دوره‌ی مالیِ تاریخ سند.
5. هم‌خوانی `account_type_id` بین گروه و زیرشاخه‌هایش.

## قدم بعدی پیشنهادی

مدل‌های SQLAlchemy برای این سه فایل اسکیما (تنانسی/i18n، RBAC، حسابداری+کارتابل) + سرویس‌های پایتونِ بالا (تخصیص شماره، تایید کارتابل، سند اختتامیه‌ی خودکار) — یا اگر ترجیح می‌دهید، اول طراحی فرم/صفحه‌ی ثبت سند حسابداری در Kivy.
