/** آدرسِ سرورِ peecha_api -- در نسخه‌یِ نهایی باید در صفحه‌یِ تنظیماتِ
 * اپ قابلِ‌تغییر باشد (هر مشتری سرورِ خودش را دارد)؛ فعلاً یک مقدارِ
 * ثابتِ قابلِ‌بازنویسی با متغیرِ محیطیِ بیلد است. */
export const API_BASE_URL = process.env.PEECHA_API_BASE_URL ?? "http://localhost:8000";
