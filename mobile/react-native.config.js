/** طبقِ درخواستِ صریحِ کاربر («ادامه بده با فونتِ وزیر»): فونتِ فارسیِ
 * Vazirmatn (پکیجِ npm رسمی، مجوزِ SIL OFL) به‌جایِ فونتِ سیستم.
 *
 * محدودیتِ شناخته‌شده: این پیکربندی فقط محلِ فایل‌هایِ فونت را اعلام
 * می‌کند -- کپیِ واقعیِ آن‌ها به پروژه‌یِ نیتیوِ اندروید/iOS (که این
 * فایل را می‌خواند) نیازمندِ اجرایِ `npx react-native-asset` (یا لینکِ
 * دستی: Info.plist + Xcodeِ iOS، android/app/src/main/assets/fonts در
 * اندروید) رویِ یک ماشینِ دارایِ Android SDK/Xcode است -- این سندباکس
 * هیچ‌کدام را ندارد (همان محدودیتِ مستندشده برایِ react-navigation در
 * App.tsx). این کار فقط یک‌بار، در اولین بیلدِ واقعیِ اپ، لازم است. */
module.exports = {
  assets: ["./assets/fonts"],
};
